# Install

`npm install` || `npm i`

---

# Things to add

- Create a `.env` file in config folder and add the following as `key = value`
  - PORT = 8000
  - DB_STRING = `your database URI`

---

# Run

`node server.js`
